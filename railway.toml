[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Disable healthcheck because this is a Telegram bot (Polling) 
# and doesn't expose a web server.
[deploy.healthcheck]
enabled = false
